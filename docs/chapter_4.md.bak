# Example: Mutex

```
klass Poison(entity):
  onTick()
    entity.health -= 1

klass Missile:
  onCollide(entity):
    lock(entity)
      entity.health -= 5
```

Impossible to write in Rust, Rust's `Mutex` *owns* its data

```rust
pub struct Mutex<T> {
  data: T
}
```

